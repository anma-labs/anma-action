# Generated by `anma sync`.
repos:
  - repo: local
    hooks:
      - id: anma-check
        name: anma check (module boundaries)
        entry: anma check
        language: system
        pass_filenames: false
